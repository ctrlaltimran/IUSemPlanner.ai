(function(){try{var TARGET = 'https://ctrlaltimran.com/IUSemPlanner/';var tables = document.querySelectorAll('table');if(!tables || tables.length === 0){alert('IUSemPlanner\n\nNo course tables found.\n\nOpen your IULMS Student Information Center(SIC)course list page,then click this bookmark.');return;}var lines = [];tables.forEach(function(table){var rows = table.querySelectorAll(':scope > tbody > tr,:scope > tr');if(rows.length === 0)return;var firstText =(rows[0].innerText || rows[0].textContent || '').trim();var semMatch = firstText.match(/^Semester\s*-\s*\d+/i);var elecMatch = firstText.match(/^Electives/i);if(!semMatch && !elecMatch)return;lines.push(semMatch ? semMatch[0] : 'Electives');for(var i = 1;i < rows.length;i++){var rowText =(rows[i].innerText || rows[i].textContent || '').trim();if(!rowText)continue;rowText = rowText.replace(/\n\t?(\d+)\t/g,'\t$1\t');rowText = rowText.replace(/\n([A-Z]{2,5}[-\s]?\d{2,4}(?:-L)?\t)/g,'\n$1');var parts = rowText.split('\n');for(var j = 0;j < parts.length;j++){var p = parts[j].trim();if(p)lines.push(p);}}});if(lines.length === 0){alert('IUSemPlanner\n\nNo IULMS semester data found on this page.\n\nMake sure you are on the SIC course list page with all 8 semesters visible.');return;}var text = lines.join('\n');var encoded = btoa(unescape(encodeURIComponent(text)));var url = TARGET + '#import=' + encoded;var win = window.open(url,'_blank');if(!win || win.closed || typeof win.closed === 'undefined'){if(confirm('IUSemPlanner: Pop-up blocked.\n\nRedirect this tab instead?')){window.location.href = url;}}}catch(err){alert('IUSemPlanner error:\n' + err.message + '\n\nMake sure you are on the IULMS course list page.');}})();